{
  "name": "project",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "node ./project/app.js",
    "dev": "cd project && npm run dev",
    "postinstall": "cd project && npm install"
  }
}
